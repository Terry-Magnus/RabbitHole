# Rabbit Hole

## Getting started

```bash
npm install
docker compose up -d
npm run dev
```

- `apps/web` → http://localhost:3000
- `services/api` → http://localhost:4000

See `context/` for product, architecture, and build-plan documentation.
